// Function that automates a greeting toward a user

// Better way to write this?
const userName = "Amahle"

function greetings() {
    alert(`Hello ${userName}, welcome back to portshare!`);
}

greetings();

function lorry(color) {
    return `${color} is the color of the lorry`;
}

alert(lorry("Blue"));

// Parameter is the variable, the argument is the values you shove into the variable